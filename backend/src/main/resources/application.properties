server.port=8080
spring.application.name=portfolio-backend

# Management / Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Logging
logging.level.com.harishraj.portfolio=INFO
